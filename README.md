# Student-Performance-Analysis
Applied EDA which revealed weak correlation among features. Performed feature engineering(polynomial interactions, binning). Tested multiple ML models(Logistic Regression, Decision Tree, Random Forest). Conducted Independent t-test to check if study hours differ significantly b/w pass/fail. Despite extensive modeling, accuracy stagnates around 50%
